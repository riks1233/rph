<?php

$main_config = [
    'https_required' => true,
    'is_dev_environment' => false,

    'mysql_host' => '',
    'mysql_dbname' => '',
    'mysql_username' => '',
    'mysql_password' => '',
];

// Show errors for developers.
if ($main_config['is_dev_environment']) {
    error_reporting(E_ALL);
    ini_set('display_errors', 1);
}

?>
